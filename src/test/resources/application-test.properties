# Embedded MongoDB Configuration for Tests
de.flapdoodle.mongodb.embedded.version=7.0.2

# Spring Data MongoDB Test Configuration
spring.data.mongodb.database=customerdb_test
spring.data.mongodb.host=localhost
spring.data.mongodb.port=0

# Logging Configuration for Tests
logging.level.com.example.customerservice=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.de.flapdoodle.embed.mongo=INFO

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
